spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        enabled: true
      config:
        server-addr: localhost:8848
        file-extension: yaml
        username: nacos
        password: nacos
        enabled: true